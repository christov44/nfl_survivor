<h1> NFL Teams </h1>

<a href="/home">Home</a> <br />

<%= link_to "Create Team", new_team_path %> <br />

<%= link_to "Matchups", matchups_path %> <br />


<table>
  <tr>
    <th>Team Name</th>
  </tr>
  <br />
  <% @teams.each do |team| %>
    <tr>
      <td> <%= team.name %> </td>
      <td> || <%= link_to "edit", edit_team_path(team) %> || </td>
      <td> <%= link_to "delete", team_path(team), method: :delete, data: {confirm: "Are you sure you want to delete?"} %></td>
    </tr>
  <% end %>
</table>
